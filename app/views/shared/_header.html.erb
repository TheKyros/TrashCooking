<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      
      
      
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>
          <a class="navbar-brand" href="<%= root_path %>">TrashCooking</a>
        </li>

        <li class="food">
          <a href="<%= categories_path %>" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Catégories d'ingredients <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Liste categories', categories_path %></li>

            <%if current_user && current_user.isadmin %>

            <li><%= link_to 'Nouvelle categorie', new_category_path %></li>
            <%end%>
          </ul>
        </li>
          <li class="meal">
          <a href="<%= recipes_path %>" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Recettes <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to 'Liste recettes', recipes_path %></li>
            <li><%= link_to 'Nouvelle recette', new_recipe_path %></li>
          </ul>
        </li>
        <li class="users">

            <% if user_signed_in? %>
                      <a href="<%= categories_path %>" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"> <%= "Bonjour #{current_user.email}" %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            
            <li>
              <%if current_user && current_user.isadmin%>
                <%= link_to 'Zone admin', admin_root_path %>
              <%end%>
              
            </li>
              <li>
              <%= link_to 'Se déconnecter', destroy_user_session_path, method: :delete %>
              </li>
            <% else %>
                <%= link_to 'Se connecter', new_user_session_path %>
            <% end %>
        </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>


